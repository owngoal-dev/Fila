No change to the app itself.

Debug symbols are now published with every release: `Fila_0.5.9_dSYMs.zip` matches the `.deb` and `.tipa` builds, and `Fila_0.5.9_sandboxed_dSYMs.zip` matches the `.ipa`. Use them to symbolicate crash reports from this version.
